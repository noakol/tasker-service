export enum Task {
  Sum = 'sum',
  Concat = 'concat',
}

export enum AvailableQueues {
  TaskerIn = 'TaskerIn',
}

export enum AvailableExchanges {
  TaskerExchange = 'TaskerExchange',
}

export enum Collection {
  Tasks = 'tasks',
}
